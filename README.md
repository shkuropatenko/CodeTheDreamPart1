# 🎧 Spotify Mini Project

A small frontend project that displays Spotify artist info and top tracks using the Spotify Web API.

## 🚀 How to run

1. Clone the repository:
```bash
git clone https://github.com/your-username/spotify-mini-project.git
cd spotify-mini-project

## 🛠️ Setup

1. Copy `.env.example` and create `.env`:

npm install
npm run dev

open in browser
http://localhost:5173

```bash
cp .env.example .env
